<li>
    {{name}}

    <button class="change-devoured" data-id="{{id}}" data-newdevour="{{devoured}}">
        {{#if devoured}}Eaten!{{else}}Remake!{{/if}}
    </button>
    <button class="delete-burger" data-id="{{id}}">
        X
    </button>
</li>